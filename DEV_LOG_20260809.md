# 大道庵 DEV_LOG (profile 11 — 海丰县大道庵)

## 2026-08-09 波哥 + Hermes

### ✅ 修复 (Restore)
- **DB connection 写死密码**: pm2 fork 子进程不读 cwd .env, 临时写死 `db/connection.js` (`DdaHaifeng@2026!GF` + socket 3306), 标注待回滚。
- **nginx `/temple/` 404**: 加 `try_files $uri $uri/ /temple/index.html;`
- **nginx WebSocket upgrade**: `/api/` 段加 `proxy_http_version 1.1` + `Upgrade` + `Connection "upgrade"`, 使 `/ws/temple/karaoke/:sid` 通过 80 端口。
- **首页加载失败 (Access denied for user 'gdq'@'localhost')**: DB 密码写死后通。

### 🚀 上线 (Deliver)
- **卡拉OK 主持端 SPA**: bundle 从 app.js 抽出到独立 `/temple/karaoke.js` (19KB, 409 行), 在 index.html `<script>` 之前加载。
  - 实测 `/#/karaoke` 显示 3 部经典, 点心经 → Session #3 已建, WS 显示"已连接"。
  - 9 个控制按钮齐全: 上一句 / 开始-暂停切换 / 下一句 / 0.5x 1.0x 1.5x / 拼音 / 字号 / 结束 / 复制链接 / 打开大屏。
- **卡拉OK 后端**: 6 个 REST API + 1 个 WebSocket, 全部 200 OK。
  - 3 部经典 + 《心经》52 句完整原文+拼音 已 seed。
- **牌位扫码**: schema + 数据 + API + 前端 全部跑通。
  - seed 3 个祖先 (陈公讳德山 / 林氏淑贞 / 黄公讳海涛) + 3 个牌位 (DDA-A-001/002, DDA-B-005)。
  - `GET /api/temple/public/casket/DDA-A-001` → 200 + 完整祖先生平 + 位置 + 祭文。
  - **scan_count 自动累加** (浏览器每次访问 logScan), 已实测累计 7+ 次。
  - **真实案例模块** 在 `/#services` 业务页底部: "📋 真实案例演示" 显示 3 个牌位, 点开跳转到 `/#/casket/CODE`, 100% 真实渲染。

### 🔧 待回滚 / 改进 (Follow-up)
- **db/connection.js 写死密码** 待 `ecosystem.config.cjs` 加 `env: { DB_PASSWORD }` 后回滚。
- **RBAC permissions** 待 INSERT (AGENTS.md 铁律未达)。
- **大屏端 / 信众端** bundle 函数已写 (`renderKaraokeDisplay` / `renderKaraokeJoin`), 待实测 2 路同步。
- **nginx Cache-Control** 临时改为 `no-cache`, 待稳定后改回 `immutable` + 改用版本号。
- **app.js 字段适配** (L487-509): API 返 `ancestor.name/casket_code/hall_name` → 前端字段 `name/code/hall`, 已在 `bindCasketDetail()` 加适配层。

### 📁 文件清单
- `/home/gdq/dist/temple/app.js` (44KB)
- `/home/gdq/dist/temple/karaoke.js` (19KB)
- `/home/gdq/dist/temple/index.html` (含 cache-bust v=20260809-1200)
- `/home/gdq/server/db/connection.js` (应急写死)
- `/home/gdq/server/routes/temple.js` (含 `/public/casket/:code` + `/public/home`)
- `/home/gdq/server/routes/temple-karaoke.js` (6 REST + 1 WS)
- `/etc/nginx/conf.d/dda.gdqshop.cn.conf` (temple alias + WS upgrade)
- `/home/gdq/server/.env` (pm2 不读, 待 ecosystem config)